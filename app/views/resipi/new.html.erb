<h1>Creating Recipe</h1>

<%= form_for :resipi, url: resipi_index_path do |f| %>
	<% if @resipi.errors.any? %>
	<div id="error_explanation">
		<h2><%= pluralize(@resipi.errors.count, "error") %> prohibited this resipi from being saved:</h2>
		<ul>
			<% @resipi.errors.full_messages.each do |msg| %>
			<li><%= msg %></li>
			<% end %>
		</ul>
	</div>
	<% end %>
	<p>
		<%= f.label :title %><br>
		<%= f.text_field :title %>
	</p>

	<p>
		<%= f.label :text %><br>
		<%= f.text_area :text %>
	</p>

	<p>
		<%= f.file_field :image %>
	</p>

	<p>
		<%= f.submit %>
	</p>
<% end %>

<%= link_to 'Back', resipi_index_path %>